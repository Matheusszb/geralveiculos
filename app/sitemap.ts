import type {MetadataRoute} from 'next';import {getVehicles} from '@/lib/vehicles';export default async function sitemap():Promise<MetadataRoute.Sitemap>{const origin=process.env.NEXT_PUBLIC_SITE_URL||'https://geralveiculos.com.br';const routes=['','/veiculos','/sobre','/venda-seu-veiculo','/financiamento','/contato'].map(x=>({url:origin+x,lastModified:new Date()}));const vehicles=await getVehicles();return[...routes,...vehicles.filter(v=>v.status==='available').map(v=>({url:`${origin}/veiculos/${v.slug}`,lastModified:new Date(v.created_at)}))]}
