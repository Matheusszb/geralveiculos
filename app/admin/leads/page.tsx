import {AdminShell} from '@/components/admin-shell';import {createClient} from '@/lib/supabase/server';type Lead={id:string;name:string;phone:string;type:string;message:string|null};export default async function Leads(){let leads:Lead[]=[];if(process.env.NEXT_PUBLIC_SUPABASE_URL){const db=await createClient();const{data}=await db.from('leads').select('id,name,phone,type,message').order('created_at',{ascending:false});leads=(data||[])as Lead[]}return <AdminShell><div className="eyebrow">Relacionamento</div><h1 className="display" style={{fontSize:58,margin:'8px 0 30px'}}>Leads</h1><table className="admin-table"><thead><tr><th>Nome</th><th>Telefone</th><th>Tipo</th><th>Mensagem</th></tr></thead><tbody>{leads.map(l=><tr key={l.id}><td>{l.name}</td><td>{l.phone}</td><td>{l.type}</td><td>{l.message}</td></tr>)}</tbody></table>{!leads.length&&<p className="text-muted">Os contatos enviados pelo WhatsApp não ficam retidos no site.</p>}</AdminShell>}
