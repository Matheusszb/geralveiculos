import {notFound} from 'next/navigation';import {AdminShell} from '@/components/admin-shell';import {AdminVehicleForm} from '@/components/admin-vehicle-form';import {createClient} from '@/lib/supabase/server';import type {Vehicle} from '@/lib/types';export default async function EditVehicle({params}:{params:{id:string}}){if(!process.env.NEXT_PUBLIC_SUPABASE_URL)return notFound();const db=await createClient();const{data}=await db.from('vehicles').select('*,vehicle_images(id,url,position)').eq('id',params.id).single();if(!data)return notFound();return <AdminShell><div className="eyebrow">Estoque</div><h1 className="display" style={{fontSize:58,margin:'8px 0 30px'}}>Editar veículo</h1><AdminVehicleForm vehicle={data as Vehicle}/></AdminShell>}
